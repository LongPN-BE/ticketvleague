import React from "react";
import { FormControl, FormGroup } from "react-bootstrap";

function LoginPage(){
    <FormGroup>
        <FormControl type="text" >
       </FormControl>
       <FormControl type="text"  >
       </FormControl>
    </FormGroup>

    return LoginPage;
}
